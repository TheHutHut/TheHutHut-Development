import theme from 'src/styles/theme';

const typography = {
  color: theme.color.text,
  h1: {
    color: theme.color.text,
    fontSize: '3.75rem',
    fontFamily: ['Belleza', 'sans-serif', '-apple-system', 'Roboto'].join(','),
  },
  h2: {
    color: theme.color.text,
    fontFamily: ['Belleza', 'sans-serif', '-apple-system', 'Roboto'].join(','),
  },
  h3: {
    color: theme.color.text,
    fontFamily: ['Belleza', 'sans-serif', '-apple-system', 'Roboto'].join(','),
  },
  h4: {
    color: theme.color.text,
    fontFamily: ['Belleza', 'sans-serif', '-apple-system', 'Roboto'].join(','),
  },
  h5: {
    color: theme.color.text,
    fontFamily: ['Belleza', 'sans-serif', '-apple-system', 'Roboto'].join(','),
  },
  h6: {
    color: theme.color.text,
    fontFamily: ['Belleza', 'sans-serif', '-apple-system', 'Roboto'].join(','),
  },
  subtitle1: {
    color: theme.color.text,
    fontFamily: ['Belleza', 'sans-serif', '-apple-system', 'Roboto'].join(','),
  },
  subtitle2: {
    color: theme.color.text,
    fontFamily: ['Belleza', 'sans-serif', '-apple-system', 'Roboto'].join(','),
  },
  body1: {
    color: theme.color.text,
    fontFamily: ['Montserrat', 'sans-serif', '-apple-system', 'Roboto'].join(
      ','
    ),
  },
  body2: {
    color: theme.color.text,
    fontWeight: 'bold',
    fontFamily: ['Montserrat', 'sans-serif', '-apple-system', 'Roboto'].join(
      ','
    ),
  },
  button: {
    fontFamily: ['Montserrat', 'sans-serif', '-apple-system', 'Roboto'].join(
      ','
    ),
  },
  link: {
    color: theme.color.text,
    fontFamily: ['Montserrat', 'sans-serif', '-apple-system', 'Roboto'].join(
      ','
    ),
  },
  caption: {
    color: theme.color.text,
    fontFamily: ['Montserrat', 'sans-serif', '-apple-system', 'Roboto'].join(
      ','
    ),
  },
  overline: {
    color: theme.color.text,
    fontFamily: ['Montserrat', 'sans-serif', '-apple-system', 'Roboto'].join(
      ','
    ),
  },
};

export default typography;
