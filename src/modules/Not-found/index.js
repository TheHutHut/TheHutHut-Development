import React from 'react';

const ModuleNotFound = () => (
    <div>
        {/* Module {props.module} is not defined. Add it to load-module.js */}
        This page could not be found.
    </div>
);

export default ModuleNotFound;
